
## 2026-03-15 17:07 SAST — Mission Control rebuild: Discovery + Scribe

- Ethan requested a full reset/rebuild of Mission Control into a simpler flow.
- Mission Control was rebuilt around Discovery first, then expanded with a Scribe tab.
- Discovery now has robust step tracing and explicit failure states (`failed_no_results` instead of false success).
- Major debugging breakthrough: search engines returned redirect wrapper URLs that were being discarded.
  - Implemented URL unwrapping for DuckDuckGo (`uddg`) and Bing (`ck/a ... u=`) links.
- Added layered discovery engines/fallbacks:
  - Playwright rendered search (Google/DDG/Bing)
  - HTML parsers
  - lynx DDG fallback
  - Scrapling-based search stage
- Added per-run raw debug logs for discovery at:
  - `/home/ethan/.openclaw/mission-control/frontend/logs/discovery-<id>.jsonl`
- Added job IDs for discovered companies (`JOB-######`).
- Added Scribe normalization workflow:
  - New Scribe tab in UI
  - Job dropdown + run button
  - Extracts/normalizes company data from discovery + sources
  - Stores normalized results in DB (`scribe_profiles` table)
- Recent key commits in `~/.openclaw/mission-control`:
  - `bc3e880` Unwrap DuckDuckGo and Bing redirect URLs in discovery parsers
  - `9887124` Add per-run discovery raw debug logging for parser diagnostics
  - `393205d` Add Scrapling-based search stage
  - `1520601` Add Scribe tab with job-id based normalization workflow
- Operational constraint observed: elevated/root tool access is blocked in this webchat provider context.
