
## 2026-03-07 13:19 SAST — Mission Control overhaul progress (durable)

- Installed ClawHub skill `smart-web-scraper` into workspace and fixed Mission Control skill installer/update logic:
  - ClawHub URLs now use zip-download/extract path (instead of git clone), with git fallback for GitHub sources.
  - Added skill source-type badge in UI (`clawhub-zip`, `git`, `local`, `system`) and source URL visibility.
- Diagnosed and fixed frontend stale-asset/MIME errors on `mc.adamslabs.co.za` caused by mismatched Next process/assets; restarted cleanly.
- Added Company Intel pipeline foundation:
  - Kickoff UI + API endpoint to queue Architect orchestration for one company.
  - Persistent run tracking and detail APIs.
  - Results UI with timeline, editable summary/profile, status updates.
  - Phase 3 polish: JSON validation/prettify, downloads, outreach draft generator.
- Added stalled-job visibility for Company Intel runs:
  - Last-update age and stale indicators in UI.
  - Auto-refresh on selected run.
- Began full ground-up rebuild direction requested by user:
  - Purged Mission Control runtime data/queue audit baseline.
  - Added roadmap/contract docs in mission-control repo.
  - Removed legacy backend folder (`mission-control/backend`) to simplify to single Next API surface.
  - Added mission config file: `mission-control/config/mission-control.json`.
- Agent/config changes applied:
  - OpenClaw bindings reduced to architect-only for WhatsApp inbound routing.
  - Scout granted workspace-only fs writes (for evidence capture) while keeping runtime denied.
  - Added cron placeholder jobs for discovery + stalled-run watchdog (disabled by default).
- Phase 1 implemented:
  - New workflow tables and lifecycle primitives (`workflow_runs`, `workflow_steps`, `workflow_blockers`, `workflow_approvals`).
  - New APIs:
    - `GET/POST /api/workflows/runs`
    - `GET /api/workflows/runs/[runId]`
    - `POST /api/workflows/tick`
    - `POST/PATCH /api/workflows/blockers`
    - `POST /api/workflows/stalls/scan`
    - `POST /api/workflows/runs/[runId]/approve-outreach` (owner-only gate)
  - Added stall detection fields (`last_heartbeat_at`, `stalled_at`) and logic.
- Phase 1c UI shipped:
  - New Workflows tab with plain-language operational status, blocker controls, stall scan, and approval gate panel.
  - Boards marked as legacy/de-emphasized path.
- Added visible build/version stamp to UI for deployment verification:
  - Bottom-right badge shows `MC v<version> · <git-sha>`.
- Current accepted direction from user:
  - Complete UI overhaul, plain-language-first (no jargon).
  - Workflow-first operations for lead discovery -> research -> mock build -> deploy -> owner WhatsApp notify -> owner-only outreach approval.
