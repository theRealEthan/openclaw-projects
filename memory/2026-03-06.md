
## 2026-03-06 19:13 SAST — Durable memory flush

- Mission Control major upgrades implemented and validated in `/home/ethan/.openclaw/mission-control`:
  - Leads exclusion management (CRUD), default exclusions (`pick n pay`, `engen`), and exclusion-aware lead normalization.
  - Moved `Run lead flow` control into Leads tab.
  - Fixed production breakages (asset hash mismatch after deploy/restart and SQLite migration order bug for `status` column).
  - Added richer Agents control center:
    - detailed agent panel
    - direct dispatch prompt creating queue jobs
    - `/api/agents/[agentId]` and `/api/agents/[agentId]/invoke`.
  - Boards v2 improvements:
    - interactive node-link flow view for job handovers
    - verbose per-job timeline
    - queue job controls (dispatch/start/stop/review/complete/cancel)
    - plain-language dashboard guidance for readability.
  - Relay auto-run implemented:
    - starting Relay-owned queue jobs now auto-sends WhatsApp via `openclaw message send`
    - emits `relay_autorun_started` and `relay_whatsapp_sent` events (`Mailer -> WhatsApp` handover)
    - auto-completes queue job on success (or moves to waiting on failure).

- User preference/decision captured:
  - Keep repo layout as **umbrella + nested repos** (not flattened):
    - `~/.openclaw` umbrella
    - `~/.openclaw/mission-control`
    - `~/.openclaw/projects`
    - `~/.openclaw/workspace`

- Git state checkpoints created in mission-control today:
  - `61f8cc7` Agents control center
  - `a819e5a` Boards v2 flow/controls APIs
  - `4a1cf1b` Node-link canvas + UX readability pass
  - `4c9bcd5` Relay auto-run on start with WhatsApp send + auto-complete

- End-to-end validation performed:
  - `workspace-health-check` jobs executed with per-agent outputs persisted at:
    - `/home/ethan/.openclaw/reports/workspace-health-check.md`
    - `/home/ethan/.openclaw/reports/workspace-health-check-output.json`
  - Relay test job `J-20260306163357-RELAY` confirmed reaching DONE with `relay_whatsapp_sent` event and `Mailer -> WhatsApp` handover.
